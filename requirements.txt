# Vercel's Python runtime installs from requirements.txt at the repo
# root — this must stay in sync with backend/pyproject.toml's
# [project.dependencies], which is the source of truth for local dev
# (`pip install -e .` from backend/). Both are pinned to the exact
# same versions deliberately (see backend/pyproject.toml's comment on
# why exact pins, not ranges).
#
# uvicorn is omitted here — Vercel's Python runtime provides its own
# ASGI server, uvicorn is only needed for local `uvicorn app.main:app`.
fastapi==0.141.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
pandas==3.0.2
openpyxl==3.1.5
python-calamine==0.8.2
numpy==2.4.4
scipy==1.17.1
statsmodels==0.14.6
langgraph==1.2.10
langchain==1.3.14
langchain-openai==1.4.1
langsmith==0.10.16
psycopg[binary]==3.3.4
